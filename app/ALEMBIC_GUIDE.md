# 🔄 Alembic - Управление миграциями БД

## Что это?

**Alembic** - это инструмент для версионного контроля схемы базы данных. Вместо ручных SQL-скриптов, все изменения БД версионируются и применяются автоматически.

## Преимущества

✅ **Версионность** - каждое изменение БД в Git
✅ **Откат** - можно вернуться к предыдущей версии
✅ **Автогенерация** - Alembic сам видит изменения в моделях
✅ **Безопасность** - миграции можно проверить перед применением
✅ **Совместная работа** - конфликты миграций решаются через Git

## Быстрый старт

### 1. Первое применение миграций

После обновления проекта до AI Hub:

```bash
# Применить все миграции
./migrate.sh upgrade

# Или через Makefile
make migrate

# Или через Docker напрямую
docker compose exec bot alembic upgrade head
```

Это создаст новые таблицы `health_data` и `ai_interactions`, а также добавит новые колонки в существующие таблицы.

### 2. Проверка текущей версии

```bash
./migrate.sh current
# Или
make migrate-current
```

Должно показать: `001_ai_hub (head)`

## Основные команды

### Применение миграций

```bash
# Применить все новые миграции
./migrate.sh upgrade
make migrate

# Применить до конкретной версии
docker compose exec bot alembic upgrade 001_ai_hub
```

### Откат миграций

```bash
# Откатить последнюю миграцию
./migrate.sh downgrade
make migrate-downgrade

# Откатить 3 миграции
./migrate.sh downgrade 3
docker compose exec bot alembic downgrade -3

# Откатить до базы (ОСТОРОЖНО!)
docker compose exec bot alembic downgrade base
```

### История и статус

```bash
# Посмотреть все миграции
./migrate.sh history
make migrate-history

# Текущая версия БД
./migrate.sh current
make migrate-current
```

### Создание новых миграций

```bash
# Автогенерация после изменения моделей
./migrate.sh create "add user preferences"
make migrate-create MSG="add user preferences"

# Вручную (пустая миграция)
docker compose exec bot alembic revision -m "custom migration"
```

## Workflow разработки

### Сценарий 1: Добавление нового поля

1. **Изменяете модель** в `database/database.py`:

```python
class User(Base):
    __tablename__ = 'users'
    
    # ... существующие поля ...
    
    # Новое поле
    timezone = Column(String(50), default='UTC')
```

2. **Создаете миграцию**:

```bash
make migrate-create MSG="add timezone to user"
```

3. **Проверяете** сгенерированный файл в `alembic/versions/`:

```python
def upgrade() -> None:
    op.add_column('users', 
        sa.Column('timezone', sa.String(length=50), nullable=True))

def downgrade() -> None:
    op.drop_column('users', 'timezone')
```

4. **Применяете**:

```bash
make migrate
```

### Сценарий 2: Добавление новой таблицы

1. **Создаете модель** в `database/database.py`:

```python
class UserSettings(Base):
    __tablename__ = 'user_settings'
    
    id = Column(Integer, primary_key=True)
    user_id = Column(BigInteger, nullable=False)
    theme = Column(String(20), default='light')
    language = Column(String(10), default='ru')
    created_at = Column(DateTime, default=datetime.utcnow)
```

2. **Создаете миграцию**:

```bash
make migrate-create MSG="add user settings table"
```

3. **Применяете**:

```bash
make migrate
```

Alembic автоматически сгенерирует код создания таблицы!

### Сценарий 3: Изменение типа колонки

1. **Создаете миграцию вручную**:

```bash
docker compose exec bot alembic revision -m "change calories to bigint"
```

2. **Редактируете** файл в `alembic/versions/`:

```python
def upgrade() -> None:
    op.alter_column('calorie_entries', 'calories',
        existing_type=sa.Integer(),
        type_=sa.BigInteger(),
        existing_nullable=False)

def downgrade() -> None:
    op.alter_column('calorie_entries', 'calories',
        existing_type=sa.BigInteger(),
        type_=sa.Integer(),
        existing_nullable=False)
```

3. **Применяете**:

```bash
make migrate
```

## Production Workflow

### Для безопасного применения на продакшене:

```bash
# 1. Создайте бэкап БД
docker compose exec postgres pg_dump -U fitbud_user fitbud_bot > backup_before_migration_$(date +%Y%m%d).sql

# 2. Проверьте, какие миграции будут применены
docker compose exec bot alembic history

# 3. Протестируйте на копии БД (если возможно)

# 4. Примените миграции
make migrate

# 5. Проверьте, что всё работает
docker compose logs -f bot

# 6. Если что-то сломалось - откатите
make migrate-downgrade
# или восстановите из бэкапа
cat backup_before_migration_20250210.sql | docker compose exec -T postgres psql -U fitbud_user -d fitbud_bot
```

## Структура проекта

```
fitbud_bot/
├── alembic/                    # Директория миграций
│   ├── versions/              # Файлы миграций
│   │   └── 001_ai_hub.py     # Миграция AI Hub
│   ├── env.py                # Конфигурация Alembic
│   ├── script.py.mako        # Шаблон для новых миграций
│   └── README
├── alembic.ini                # Настройки Alembic
├── migrate.sh                 # Скрипт-обертка для команд
└── database/
    └── database.py           # Модели SQLAlchemy
```

## Автогенерация миграций

Alembic умеет **автоматически определять изменения** между моделями и БД:

```bash
# После изменения моделей
make migrate-create MSG="описание изменений"
```

Alembic создаст миграцию с нужными изменениями!

**Что обнаруживает автоматически:**
- ✅ Новые таблицы
- ✅ Новые колонки
- ✅ Удаленные таблицы/колонки
- ✅ Изменение типов колонок (иногда)
- ✅ Индексы и ограничения

**Что НЕ обнаруживает:**
- ❌ Переименование таблиц (видит как удаление + создание)
- ❌ Переименование колонок (видит как удаление + создание)
- ❌ Изменение default значений (иногда)

Для этих случаев редактируйте миграцию вручную.

## Работа в команде

### Конфликты миграций

Если два разработчика создали миграции параллельно:

```
alembic/versions/
├── 001_ai_hub.py
├── 002_developer1.py  ← Миграция 1
└── 002_developer2.py  ← Миграция 2 (конфликт!)
```

**Решение:**

1. Переименуйте одну из миграций:
```bash
mv alembic/versions/002_developer2.py alembic/versions/003_developer2.py
```

2. Исправьте `revision` и `down_revision` в файле:
```python
revision = '003_developer2'
down_revision = '002_developer1'  # Указываем предыдущую
```

3. Примените обе миграции:
```bash
make migrate
```

### Git Workflow

```bash
# 1. Создаете новую ветку
git checkout -b feature/new-model

# 2. Изменяете модели
# 3. Создаете миграцию
make migrate-create MSG="add new model"

# 4. Коммитите миграцию вместе с моделью
git add database/database.py
git add alembic/versions/004_add_new_model.py
git commit -m "Add new model with migration"

# 5. Мерджите в main
git checkout main
git merge feature/new-model

# 6. Применяете на сервере
make migrate
```

## Troubleshooting

### "Target database is not up to date"

**Проблема:** Кто-то применил миграции, которых нет у вас локально.

**Решение:**
```bash
git pull
make migrate
```

### "Can't locate revision identified by 'xxx'"

**Проблема:** В БД есть версия миграции, которой нет в коде.

**Решение:**
```bash
# Посмотрите текущую версию в БД
make migrate-current

# Откатитесь до известной версии
docker compose exec bot alembic downgrade 001_ai_hub

# Или очистите историю (ОСТОРОЖНО!)
docker compose exec postgres psql -U fitbud_user -d fitbud_bot -c "DELETE FROM alembic_version;"
```

### "Table already exists"

**Проблема:** Таблица уже создана, но миграция пытается её создать.

**Решение:**

Отметьте миграцию как примененную без выполнения:
```bash
docker compose exec bot alembic stamp head
```

### Alembic не видит изменения моделей

**Причины:**
1. Модель не импортирована в `alembic/env.py`
2. Модель не наследуется от `Base`
3. Запущен из неправильной директории

**Решение:**
```bash
# Проверьте, что модель есть в database.py
# и импортируется в alembic/env.py

# Запускайте из корня проекта
cd /opt/fitbud_bot
make migrate-create MSG="test"
```

## Best Practices

1. **Всегда создавайте бэкап** перед миграцией на продакшене
2. **Проверяйте миграции** перед применением
3. **Одна миграция = одна логическая задача**
4. **Давайте понятные имена** миграциям
5. **Коммитьте миграции вместе с кодом**
6. **Никогда не редактируйте** уже примененные миграции
7. **Пишите downgrade()** - это страховка
8. **Тестируйте откат** миграций на dev-окружении

## Примеры миграций

### Добавление индекса

```python
def upgrade():
    op.create_index('idx_user_email', 'users', ['email'])

def downgrade():
    op.drop_index('idx_user_email', 'users')
```

### Переименование колонки

```python
def upgrade():
    op.alter_column('users', 'name', new_column_name='full_name')

def downgrade():
    op.alter_column('users', 'full_name', new_column_name='name')
```

### Добавление foreign key

```python
def upgrade():
    op.create_foreign_key(
        'fk_calorie_user',
        'calorie_entries', 'users',
        ['user_id'], ['telegram_id']
    )

def downgrade():
    op.drop_constraint('fk_calorie_user', 'calorie_entries')
```

### Миграция данных

```python
from sqlalchemy.sql import table, column

def upgrade():
    # Создаем новую колонку
    op.add_column('users', sa.Column('status', sa.String(20)))
    
    # Заполняем данными
    users_table = table('users',
        column('id', sa.Integer),
        column('status', sa.String)
    )
    op.execute(
        users_table.update().values(status='active')
    )

def downgrade():
    op.drop_column('users', 'status')
```

## Интеграция с CI/CD

### GitHub Actions пример:

```yaml
- name: Run migrations
  run: |
    docker compose exec -T bot alembic upgrade head
```

### Pre-deploy checklist:

- [ ] Бэкап БД создан
- [ ] Миграции протестированы локально
- [ ] Downgrade работает
- [ ] Миграции в Git
- [ ] Docker образ пересобран

---

**Готово!** Теперь ваша БД под версионным контролем! 🎉
